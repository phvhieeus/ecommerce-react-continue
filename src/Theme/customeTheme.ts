import { createTheme } from "@mui/material";

const customeTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#E74292",
    },
    secondary: {
      main: "#EAF0F1",
    },
  },
});

export default customeTheme;
